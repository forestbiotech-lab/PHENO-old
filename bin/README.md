## Set symblink to systemd service
systemctl link /abs/path 

#Start
sudo systemctl start webServer.service

#Stop
sudo systemctl stop webServer.service

#enbable
sudo systemstl enable webServer.service

